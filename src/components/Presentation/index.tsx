import { Flex, Heading } from "@chakra-ui/react";

export default function Presentation() {
    return (
        <Flex
            py={{ base: 2 }}
            px={{ base: 4 }}
            h='100vh'
        >
            <Heading >Falicitando caminhos</Heading>
        </Flex>
    )
}